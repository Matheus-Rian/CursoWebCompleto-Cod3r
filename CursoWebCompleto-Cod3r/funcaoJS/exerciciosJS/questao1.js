function operacoesBasicas(x, y){
    let soma = x + y
    let subtracao = x - y
    let mult = x * y
    let divisao = x / y

    console.log(`Resultados:
    Soma: ${soma}
    subtração: ${subtracao}
    multiplicação: ${mult}
    divisão: ${divisao}`)
}

operacoesBasicas(4, 2)